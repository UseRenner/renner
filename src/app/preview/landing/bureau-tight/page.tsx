import { BureauTightBody } from "./BureauTightBody";

export const metadata = { title: "Landing · Bureau · Tight · Renner", robots: { index: false, follow: false } };
export const dynamic = "force-dynamic";

export default function BureauTightLanding() {
  return <BureauTightBody />;
}
